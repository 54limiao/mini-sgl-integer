# Keep this separate from MiniSGL runtime dependencies.
# llmcompressor 0.12 requires torch>=2.10 and transformers>=5.9, while MiniSGL
# runtime currently pins torch<2.10 and transformers<=4.57.3.
llmcompressor>=0.12.0,<0.13
datasets>=4.8.4,<=5.0.0
